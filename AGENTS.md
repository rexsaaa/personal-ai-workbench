# AGENTS.md

## 项目定位

`personal-ai-workbench` 定位为一套以个人为中心、跨设备复用，并与具体领域和单一 AI 解耦的 AI 原生长期项目与工作协作框架。它通过可同步的显式文档持续沉淀用户偏好、项目上下文和实践经验，使不同设备或 Agent 能够快速恢复协作状态并继续推进任务。

Agent 应结合当前用户画像、通用规则和具体项目上下文，基于用户的背景、目标与约束协助澄清问题、制定计划、推进执行和复盘；在此过程中主动识别有效协作方式、用户自我认知和可复用经验，经用户确认后沉淀到对应模块，使后续协作持续贴合用户并提高任务推进效率。

## 项目结构

```text
personal-ai-workbench/
├── AGENTS.md
├── README.md
├── rules/
├── profile/
└── projects/
```

## 默认协作规则

- 按“直接子项”的读取条件逐层加载；进入目录时先读取其 `AGENTS.md`，再按该文件路由，不默认读取目录内其他内容。
- 开始任务前，先结合用户背景和当前上下文，明确目标、约束与完成标准。
- 对会影响方案或结果的事实和意图拿不准时，不自行臆测，主动向用户确认；低风险细节可合理判断。
- 需要规划的任务，制定与任务复杂度相称的计划；简单明确的任务直接处理。
- 默认先讨论；只有用户明确要求执行、修改或落盘时，才进行对应操作。
- 协作过程中持续留意具有长期价值的信息，并在实质性任务完成或复盘时按 `rules/` 的路由检查和处理。
- 回答保持简练，优先说明结论、重要依据和需要用户确认的内容。

## 直接子项

### `README.md`

- **职责**：面向使用者说明工作台，帮助用户理解、评估和使用本项目。

### `rules/`

- **职责**：集中管理跨模块或整个工作台生效的通用规则及其加载路由。
- **读取条件**：默认读取。

### `profile/`

- **职责**：承载当前用户跨项目生效的长期信息与协作上下文，并支持持续演进。
- **读取条件**：默认读取。

### `projects/`

- **职责**：承载具体项目接入工作台所需的通用结构、管理边界和项目实例。
- **读取条件**：需要创建、接入或进入具体项目时读取。
